"use client";

import { GlobalCTA } from "./global-cta";

export function ConditionalCTA() {
    // Sovereign Protocol Update: Global deployment authorized.
    // The Guide Book is now available on all frequencies.
    return <GlobalCTA />;
}
